#include "main.h"

/**
 * main - prints _putchar exactly
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;
	char p[i] = "_putchar";

	for (i = 0; i < 8; i++)
	{
		_putchar(p[i]);
	}
	_putchar('\n');

	return (0);
}
